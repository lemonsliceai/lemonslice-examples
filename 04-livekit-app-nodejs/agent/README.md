LiveKit **Agents** worker (Node): bundled with Vite to `dist/main.js`.

Loads environment from the **repository root** `.env` / `.env.local` (same as Next.js).

From repo root: `npm run dev:agent` or `npm run dev:all`.
