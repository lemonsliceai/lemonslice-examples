{
  "name": "lemonslice-livekit-nodejs-example-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "dev": "npm run build && node dist/main.js dev",
    "download-files": "npm run build && node dist/main.js download-files",
    "start": "node dist/main.js start"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@livekit/agents": "^1.0.46",
    "@livekit/agents-plugin-lemonslice": "^1.0.46",
    "@livekit/noise-cancellation-node": "^0.1.9",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.11",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
