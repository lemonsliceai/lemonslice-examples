[project]
name = "lemonslice-green-screen-landscape-demo-agent"
version = "0.1.0"
description = "LiveKit Agents worker: LiveKit Inference LLM + ElevenLabs TTS + LemonSlice avatar"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[lemonslice]>=1.6.5",
    "livekit-plugins-elevenlabs>=1.6.5",
    "livekit-plugins-noise-cancellation>=0.2",
    "python-dotenv>=1.0.0",
]
